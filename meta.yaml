# ===================================================
# meta infos
# ===================================================
meta:
  project: kyt-cli
  include:
    - dobi.yaml
    - version.yaml
    - azure-runtime/dobi.yaml
    - kubectl-runtime/dobi.yaml
    - kyt-api-spec/dobi.yaml
    - kyt-cli/dobi.yaml
    - kyt-api-server/dobi.yaml
    - kyt-service-deployment/dobi.yaml

# ===================================================
# alias
# ===================================================

alias=build:
  tasks:
    - build-kyt-cli
    - build-kyt-api-server
    - image-kyt-api-server
  annotations:
    description: "[alias] build all"
    tags:
      - alias

alias=deploy:
  tasks:
    - image-kyt-api-server:push
  annotations:
    description: "[alias] push all"
    tags:
      - alias

alias=aks-deploy:
  tasks:
    - deploy-kyt-api-server
  annotations:
    description: "[alias] deploy all"
    tags:
      - alias
