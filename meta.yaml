# ===================================================
# meta infos
# ===================================================
meta:
  project: kyt-cli
  include:
    - dobi.yaml
    - version.yaml
    - kyt-api-spec/dobi.yaml
    - kyt-cli/dobi.yaml
    - kyt-api-server/dobi.yaml

# ===================================================
# alias
# ===================================================

alias=build:
  tasks:
    - build-kyt-cli
    - build-kyt-api-server
  annotations:
    description: "[alias] build all"
    tags:
      - alias
