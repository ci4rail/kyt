(*) = DONE

*GetDevices with page support -> isn't possible QueryDevices doesn't support paging

kyt-cli
    *Integrate kyt-cli from Heike
    *Make host configurable

Server
    *port configuration
    *corba integration
    *generator output dir go->openapi
    *generalize IoTHub SAS TOken map
    README
    Add unit tests
    GIN Release mode
    Create container
    Get SAS directly from azure (use service principal)

Repo
    Remove autogenerated files from repo
        *server
        cli
            Auto-Remove kyt-cli/openapi/go.mod/sum
    cleanup dobi files (move common entries)
    Rename Repo to kytctl(?)

Add component tests
    kyt-cli against server with dummy data
