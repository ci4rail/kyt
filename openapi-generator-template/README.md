# Adaption of the openapi-generator go-gin-server templates

Templates have been fetched with
```bash
docker run --rm -v $PWD:/out openapitools/openapi-generator-cli author template -g go-gin-server  -o /out
```
